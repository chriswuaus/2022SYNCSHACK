export type UnitCardProps = {
  unitCode: string;
  unitName: string;
  unitRating: number;
};

export type SideBarProps = {};

export type ButtProps = {
  highlighted: boolean;
  link: string;
};

export type SearchProps = {
  highlighted: boolean;
  link: string;
};
